{
  "name": "github-actions-workflow-experiment",
  "version": "0.3.3",
  "description": "test",
  "scripts": {
    "commit": "cz",
    "prepare": "husky install"
  },
  "author": "puku0x",
  "license": "MIT",
  "devDependencies": {
    "@commitlint/cli": "~17.1.0",
    "@commitlint/config-conventional": "~17.1.0",
    "@commitlint/cz-commitlint": "~17.1.0",
    "commitizen": "~4.2.0",
    "husky": "~8.0.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  },
  "config": {
    "commitizen": {
      "path": "@commitlint/cz-commitlint"
    }
  }
}
